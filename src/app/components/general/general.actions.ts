import { createAction, props } from '@ngrx/store';

export const addAction = createAction(
  '[GENERAL] Add'
);

export const subtractAction = createAction(
    '[GENERAL] Substract'
  );

//   export const refreshAction = createAction(
//   '[GENERAL] Refresh'
// ); NO FA FALTA!!! HO FA ANGULAR DE MOMENT 
